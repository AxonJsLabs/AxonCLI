import { Request, Response, nextFn } from "@axonlabs/core";

const <%= middlewareName %>Middleware = async (req: Request, res: Response, next: nextFn) => {
    next();
}

export {
    <%= middlewareName %>Middleware
}