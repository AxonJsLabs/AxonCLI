/**
 * <%= middlewareName %>Middleware
 * @param {import("@axonlabs/core").Request} req 
 * @param {import("@axonlabs/core").Response} res 
 * @param {import("@axonlabs/core").nextFn} next 
 */
const <%= middlewareName %>Middleware = async (req, res, next) => {
    next();
}


// For ModuleJs
export {
    <%= middlewareName %>Middleware
}